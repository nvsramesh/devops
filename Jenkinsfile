pipeline {
    agent {
        label 'buildserver'
    }
    stages {
        stage('checkout') {
            steps {
                
                
               checkout scm 
            
                
            }
        }
    }
}
